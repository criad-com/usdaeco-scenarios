# Changelog

## 0.9.0

- Add the usdAECO suite v0.3.0 to train aeco-0.9.0 with a shallow, nonrecursive release gate in both profiles.
- Verify suite gitlinks against origin release tags, report explicit released overrides, and inventory stage proofs and package/analysis provenance.
- Keep scenarios v0.8.1 as the released pin while checking this v0.9.0 candidate without recursion.

## 0.8.1 — public train closure

- Advance IFC, Revit, Bonsai, board and both native kit releases; retain the public dependency floors.
- Refresh the five-example walkthrough and the 24-row tagged family index.
- Preserve the 0.8.0 evidence and measure one fresh full gate plus the fast profile.
- Sweep current documentation for the retired repeated-floor repository name; retain historical evidence.
- Fix minimal-source selection for core and axis while retaining build-up's declared data-centre input; focused suites pass 71/71 and 54/54.
- Record drift 0/125; full 124 checks, 3 failed, 25 not run in 1222.26 s; fast 53 checks, 0 failed, 23 not run in 79.37 s. Preserve the original full failures under the single-run limit.

## 0.8.0 — public train

- Select the public release tags and first public floors for all 24 family entries.
- Rename the repeated-floor consumer to usdaeco-repeat and refresh public links.
- Preserve declared suite toolchains, source aliases and evaluator idempotence fixes.
- Record one fresh full gate and a separate fast-profile measurement.

## 0.7.1

- Refresh the aeco-0.7.0 releases after the dependency and source-portability fixes, retaining every dependency floor.
- Select declared flake fixtures separately from direct suite inputs; preserve historical compatibility checks without a drift waiver.
- Preserve each suite’s declared toolchain in both imports and environment variables; record the selected harness per suite.
- Retarget the ignored source alias when compatibility consumers override the pinned data release, preserving S29 archive paths.
- Run portable wall and pipe publications from their isolated source checkouts and report measured structure counts.
- Measure CCTV cache reuse against the current evaluator after any historical example receipt has been refreshed.
- Refresh the five-example walkthrough, tagged family index and full/fast acceptance evidence.

## 0.7.0

- Pin train aeco-0.7.0 with released tags and explicit inclusive dependency floors.
- Check direct pins inside the train and their own ranges; verify revisions against the declared tag. Inventory historical fixtures separately.
- Record one corrected fresh full gate: 122 checks, 2 failed, 28 not run; 92 PASS; 1415.88 s. All five setup rows pass together.
- Record fast profile: 53 checks, 1 failed, 23 not run; 29 PASS; 72.84 s. Skipped publication reproductions remain NOT RUN.
- Retain 2 exact direct-pin findings and the measured full budget failure.
- Refresh the five-use-case walkthrough and 23-row family index.

## 0.6.0

- Update the release inventory to train aeco-0.6.0, including Clash v0.2.0,
  Solid v0.1.1, Board v0.1.2 and Toolchain v0.3.4.
- Add a source-only command to clone every tagged train repository into a
  fresh family root and verify recorded revisions.
- Add strict dependency-pin, requirement-range and use-case publication
  drift checks, with seeded regression fixtures.
- Measure all five published variant manifests and the fresh outputs of the
  five new repositories; run independent suites with bounded concurrency.
- Export the CCTV, clash, plan, compliance and typical walkthrough with one
  released render per step, and generate the tagged family source-card index.
- Record a fresh 122-row gate: 90 PASS, 4 FAIL and 28 NOT RUN in 1,385 seconds.
  Preserve exact-pin drift, Solid's example timeout and the exceeded time
  budget as blocking failures. All five variant manifests and the 107-test
  scenarios regression suite pass.

## 0.5.0

- License the gate and synthetic fixtures under MIT.
- Pin train aeco-0.5.0 to the republished releases, including datacentre v0.4.5 and toolchain v0.3.3
  and the two native Solid kits.
- Audit fresh tagged sources, run newly released use-case suites, and reject
  missing repositories in any remaining placeholder rows.
- Record counts, source revisions, timings and explicit execution limits.
- Resume with corrected publication inputs and execute the five newly tagged
  use-case suites, including the IFC v0.2.0 dependency required by Route S.
- Bootstrap complete integration fixtures and stable publication layouts; keep
  consumer review images within the shared cap without changing image checks.
- Commit 106 family rows, 1,422 released-suite checks and the diagnosis of all
  14 prior failures; retain Solid's reproduced publication mismatch as a failure.

## 0.4.0

- Re-pin the released core 0.9 generation and inventory all repositories,
  including unreleased wave-2 seeds.
- Run repository checks in disposable checkouts; consume published data-centre
  stages and the CCTV-owned example, findings and render sources.
- Discover IFC and Bonsai through integration entry points; preserve explicit
  NOT RUN rows for absent examples and live execution.
- Adopt the gate skeleton, source-only testenv and public flake inputs.
- Verify current published examples separately from older fixtures required
  by released checks; report unavailable byte-qualified inputs explicitly.

## 0.3.7

- Pin generator v0.3.4 and optional exec v0.1.3; refresh the generator
  revision and immutable source hash in the Nix metadata.
- Retain the camera design, study rules and declared compatibility ranges.
  Both dependency releases refresh tested-input evidence.
- Rerun all eight library checks from an empty `.work`: 61 checks,
  0 failed; 60 PASS and only Revit NOT RUN. Both link rows pass 18/18,
  optional exec compatibility is PROVEN and all 82 scenario tests pass.
- Verify the normalized stage, eight study hashes/results, residual
  samples and camera settings equal v0.3.6; refresh acceptance and provenance.
- Record the single offline Nix attempt as not proven at the unresolved
  nested toolchain input.

## 0.3.6

- Pin CCTV v0.4.8, Sync v0.4.5 and optional exec v0.1.2; refresh runtime
  flake revisions and immutable source hashes.
- Compute optional companion compatibility from its declared ranges against
  the pinned core/CCTV releases. Report each requirement and version, with
  an offending range for NOT PROVEN. Keep native exec outside the runtime
  plugin set and never build or load it.
- Cover accepted ranges, lower/upper boundary refusals, unavailable releases
  and missing or invalid declarations in unit tests.
- Rerun all eight library checks, IFC/Bonsai cases, studies and renders:
  61 checks, 0 failed; 60 PASS and only Revit NOT RUN. Both link rows
  pass 18/18 roots, optional exec compatibility is PROVEN, and all 82
  scenario tests pass. The eight study results are unchanged; the stage differs
  only in release stamps and input hashes.
- Refresh acceptance and provenance; the single offline Nix attempt remains
  not proven at the unresolved nested toolchain input.

## 0.3.5

- Pin CCTV v0.4.7 in source dependencies and Nix metadata; audit all nine
  releases and six schema requirements.
- Run documentation links before builds and at completion. Retain failing
  file/link pairs in the acceptance table and per-root JSON, preserving an
  earlier failure when the final snapshot passes.
- Complete the isolated family layout automatically with a scenarios symlink
  to the current checkout; refuse conflicting entries without replacing them.
  The earlier 16/18 link failure was deterministic, caused by the missing
  scenarios sibling. Prior 18/18 results relied on a manually created link.
- Start with `.work` absent and rerun every family library check, IFC/Bonsai
  case, study and render, with the scenarios link created by source isolation:
  60 checks, 0 failed; 59 PASS and only Revit NOT RUN. Both link snapshots
  pass 18/18 roots, CCTV passes 123/123 and all 72 scenario tests pass.
- Refresh acceptance measurements and provenance. The single offline Nix
  attempt remains not proven at the unresolved nested toolchain input.

## 0.3.4

- Pin core v0.8.4 and CCTV v0.4.6 in source dependencies and Nix metadata;
  verify nine release identities and six compatible schema requirements.
- Rerun every family library check, IFC/Bonsai cases, studies and renders:
  59 checks, 0 failed; 58 PASS and only Revit NOT RUN. CCTV passes 123/123
  checks and all 64 scenario tests pass.
- Refresh acceptance measurements and provenance; retain the camera design,
  study rules and all three committed images. The single offline Nix attempt
  remains not proven at the unresolved nested toolchain input.

## 0.3.3

- Complete the fresh family gate: 59 checks, 0 failed; 58 PASS and only Revit
  NOT RUN. All 64 scenario tests pass; exterior coverage reaches 5/5.
- Refresh the changed overview render and retain the two unchanged look-through
  views; record current image measurements and content comparisons.
- Pin core v0.8.3, CCTV v0.4.5, Sync v0.4.4 and generator v0.3.3;
  verify all nine release identities and six schema requirements.
- Remove the Status enumeration compatibility copy and validate zero
  built-in errors on the raw conversion before kind imports.
- Accept the documented redundant column sightlines only when their targets,
  blockers, views and full fixed coverage match the pinned exception evidence.
- Refresh the native Revit reference to generator v0.3.3: 12/12 cases and
  45/45 convergence with an unmodified runner. Verify source ancestry and two
  artifact hashes separately; retain one offline DC-revit NOT RUN row.
- Build the Nix core plugin from its released manifest and refresh immutable
  source hashes. The single offline Nix closure attempt remains not proven.

## 0.3.2

- Pin core v0.8.2, CCTV v0.4.4, Sync v0.4.3 and generator v0.3.1;
  retain exact released revisions for the rest of the family.
- Audit optional exec v0.1.1 identity separately from the runtime closure.
  Evaluate schema requirements against manifest versions, independently of
  repository release tags. Refresh source hashes in the Nix lock.
- Update the facility census to 45 cameras and 2,954 phased elements.
- Remove a real lobby dome to demonstrate PTZ-only coverage on the original
  main-door samples; verify the real tray drop loses one previously covered door.
- Preserve enclosed-sample counts and coordinates/blockers for residual
  findings. Keep the strict column assertion visible when secondary sightlines
  are blocked despite complete fixed corridor coverage.
- Record offline Revit as NOT RUN with a hashed reference to released 12/12
  live cases and 45/45 optical convergence; refresh all three facility renders
  with visible sector outlines, separate envelope counts and recorded cameras.

## 0.3.0

- Pin the released generator and family libraries; audit all sources before
  isolated builds and explicitly propagate source roots to subprocesses.
- Generate the demo data centre and compose core, pipe, wall and CCTV imports.
  Author eight scoped studies, retain design findings at their error severity,
  exercise driver mutations, compare independent builds and reuse study views.
- Add camera move/aim transactions through the sync CLI, three facility views,
  image content checks, metadata sanitization and explicit unavailable-host rows.
- Keep the existing family cases and expose the gate through `check.py`.

## 0.2.0

- Add the CCTV lobby coverage cases and the production-importer roundtrip demo.
