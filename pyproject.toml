[project]
name = "usdaeco-scenarios"
version = "0.9.0"
description = "Pinned family acceptance gate"
requires-python = ">=3.11"
license = "MIT"

[project.scripts]
scenarios = "usdaeco_scenarios.cli:main"

[tool.pytest.ini_options]
testpaths = ["testenv"]
